"use client";

import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { ArrowUpRight, Check, CheckCircle2, ChevronRight, Loader2, LockKeyhole, MessageSquare, ShieldCheck } from "lucide-react";
import { GandoMark } from "@/components/gando-mark";
import { SD_CODES, SD_STAGE_META, type SD01Content, type SDCode, type SDDocumentRecord, type SDRoomBrandTheme } from "@/lib/sd-room-types";
import type { SD02Content, SD03Content, SD04Content, SD05Content } from "@/lib/sd-stage-content";

type PublicDocument = SDDocumentRecord & {
  validated_at?: string | null;
  validated_by_email?: string | null;
  validated_by_first_name?: string | null;
  validated_by_last_name?: string | null;
};

type PublicRoomData = {
  room: { id: string; title: string; companyName: string; companyLogoUrl: string | null; bannerImageUrl: string | null; theme: SDRoomBrandTheme; displayTitle: string; displaySubtitle: string; currentStage: SDCode; updatedAt: string };
  documents: PublicDocument[];
  visitorEmail: string;
  visitorFirstName: string;
  visitorLastName: string;
};

function initials(value: string) { return value.split(/\s+/).filter(Boolean).slice(0, 2).map(part => part[0]?.toUpperCase()).join("") || "CL"; }
function formatDate(value?: string | null) { if (!value) return ""; try { return new Intl.DateTimeFormat("fr-FR", { day: "2-digit", month: "long", year: "numeric" }).format(new Date(value)); } catch { return ""; } }
function Logo({ name, url, size = "md" }: { name: string; url: string | null; size?: "sm" | "md" }) { const classes = size === "sm" ? "h-9 w-9 text-[10px]" : "h-12 w-12 text-xs"; if (url) return <div className={`${classes} grid shrink-0 place-items-center overflow-hidden rounded-full border border-[#ded9ce] bg-white`}><img src={url} alt={`Logo ${name}`} className="h-full w-full object-contain p-1.5" /></div>; return <div className={`${classes} grid shrink-0 place-items-center rounded-full border border-[#ded9ce] bg-white font-bold tracking-[-0.02em] text-[#5146aa]`}>{initials(name)}</div>; }
function Eyebrow({ children }: { children: React.ReactNode }) { return <div className="text-[11px] font-semibold uppercase tracking-[0.16em] text-[#746f66]">{children}</div>; }
function EditorialSection({ title, children, kicker }: { title: string; children: React.ReactNode; kicker?: string }) { return <section className="rounded-[20px] border border-[#e7e3db] bg-white px-5 py-6 shadow-[0_1px_1px_rgba(26,33,31,0.02)] sm:px-8 sm:py-8">{kicker ? <Eyebrow>{kicker}</Eyebrow> : null}<h2 className="mt-1 text-[21px] font-semibold leading-tight tracking-[-0.025em] text-[#1a2321] sm:text-[23px]">{title}</h2><div className="mt-5 text-[16px] leading-[1.75] text-[#4d5a56]">{children}</div></section>; }
function Lead({ code, children }: { code: SDCode; children: React.ReactNode }) { return <section className="border-l-[3px] border-[#6b5fc8] py-1 pl-5 sm:pl-7"><Eyebrow>{code} · {SD_STAGE_META[code].title}</Eyebrow><p className="mt-4 max-w-[760px] text-[20px] font-medium leading-[1.55] tracking-[-0.02em] text-[#202b28] sm:text-[23px]">{children}</p></section>; }
function BulletList({ items, empty = "À confirmer ensemble" }: { items?: string[]; empty?: string }) { if (!items?.length) return <p className="italic text-[#8b938f]">{empty}</p>; return <ul className="space-y-3">{items.map((item, index) => <li key={`${index}-${item.slice(0, 20)}`} className="flex gap-3"><span className="mt-[11px] h-1.5 w-1.5 shrink-0 rounded-full bg-[#7568cf]" /><span>{item}</span></li>)}</ul>; }
function NumberedList({ items }: { items?: string[] }) { if (!items?.length) return <p className="italic text-[#8b938f]">À confirmer ensemble</p>; return <ol className="space-y-4">{items.map((item, index) => <li key={`${index}-${item.slice(0, 20)}`} className="grid grid-cols-[30px_1fr] gap-3"><span className="pt-0.5 font-mono text-[12px] font-semibold text-[#6b5fc8]">{String(index + 1).padStart(2, "0")}</span><span>{item}</span></li>)}</ol>; }
function PairGrid({ leftTitle, left, rightTitle, right }: { leftTitle: string; left: React.ReactNode; rightTitle: string; right: React.ReactNode }) { return <div className="grid gap-6 lg:grid-cols-2"><div><h3 className="mb-3 text-[13px] font-semibold uppercase tracking-[0.08em] text-[#727b77]">{leftTitle}</h3>{left}</div><div><h3 className="mb-3 text-[13px] font-semibold uppercase tracking-[0.08em] text-[#727b77]">{rightTitle}</h3>{right}</div></div>; }

function SD01Document({ content }: { content: SD01Content }) {
  const stakeholders = content.stakeholders || []; const painPoints = content.painPoints || []; const solutionFit = content.solutionFit || []; const nextSteps = content.nextSteps || [];
  return <div className="space-y-5 sm:space-y-6"><Lead code="SD01">{content.executiveSummary || "Synthèse en cours de validation."}</Lead><EditorialSection title="Contexte de l’organisation" kicker="Situation actuelle"><div className="grid gap-5 sm:grid-cols-[160px_1fr]"><div><div className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#7a837f]">Secteur</div><div className="mt-2 font-medium text-[#202b28]">{content.companyProfile?.sector || "À confirmer"}</div></div><div><div className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#7a837f]">Entreprise</div><p className="mt-2">{content.companyProfile?.description || "À compléter"}</p></div></div>{content.companyProfile?.context ? <p className="mt-5 border-t border-[#eeeae2] pt-5">{content.companyProfile.context}</p> : null}{content.gandoContext ? <p className="mt-5 rounded-xl bg-[#f5f2ea] px-4 py-4"><strong className="font-semibold text-[#2a3431]">Point de rencontre avec Gando.</strong> {content.gandoContext}</p> : null}</EditorialSection><EditorialSection title="Personnes clés" kicker="Parties prenantes">{stakeholders.length ? <div className="divide-y divide-[#eeeae2]">{stakeholders.map((person, index) => <div key={`${person.name}-${index}`} className="grid gap-2 py-4 first:pt-0 last:pb-0 sm:grid-cols-[1fr_1fr]"><div><div className="font-semibold text-[#202b28]">{person.name}</div><div className="mt-1 text-[14px] text-[#6b5fc8]">{person.role || "Rôle à confirmer"}</div></div><div className="text-[14px] text-[#69736f]">{person.organization}{person.notes ? <><br />{person.notes}</> : null}</div></div>)}</div> : <p className="italic text-[#8b938f]">Cartographie à compléter.</p>}</EditorialSection><EditorialSection title="Processus actuel" kicker="Comment cela fonctionne aujourd’hui"><NumberedList items={content.currentProcess} /></EditorialSection><EditorialSection title="Enjeux prioritaires" kicker="Ce qui doit changer">{painPoints.length ? <div className="space-y-5">{painPoints.map((pain, index) => <article key={`${pain.title}-${index}`} className="grid gap-3 border-b border-[#eeeae2] pb-5 last:border-0 last:pb-0 sm:grid-cols-[44px_1fr]"><div className="font-mono text-[12px] font-semibold text-[#6b5fc8]">P{pain.priority || index + 1}</div><div><h3 className="font-semibold leading-6 text-[#202b28]">{pain.title}</h3><div className="mt-2"><BulletList items={pain.details} /></div></div></article>)}</div> : <p className="italic text-[#8b938f]">Enjeux à préciser.</p>}</EditorialSection><EditorialSection title="Réponse envisagée" kicker="Correspondance besoin / solution">{solutionFit.length ? <div className="divide-y divide-[#eeeae2]">{solutionFit.map((item, index) => <div key={index} className="grid gap-3 py-5 first:pt-0 last:pb-0 md:grid-cols-[0.9fr_1.1fr]"><div className="font-semibold leading-6 text-[#202b28]">{item.need}</div><div>{item.response}</div></div>)}</div> : <p className="italic text-[#8b938f]">Périmètre à confirmer.</p>}</EditorialSection><EditorialSection title="Décisions et prochaines étapes" kicker="Alignement"><PairGrid leftTitle="Décisions actées" left={<BulletList items={content.decisions} />} rightTitle="Plan d’action" right={<BulletList items={nextSteps.map(step => `${step.owner || "À définir"} — ${step.action}${step.dueDate ? ` · ${step.dueDate}` : ""}`)} />}/>{content.openQuestions?.length ? <div className="mt-6 border-t border-[#eeeae2] pt-5"><h3 className="mb-3 text-[13px] font-semibold uppercase tracking-[0.08em] text-[#727b77]">Questions encore ouvertes</h3><BulletList items={content.openQuestions} /></div> : null}</EditorialSection></div>;
}
function SD02Document({ content }: { content: SD02Content }) { return <div className="space-y-5 sm:space-y-6"><Lead code="SD02">{content.objective || "Plan d’action mutuel à finaliser."}</Lead><EditorialSection title="Définition du succès" kicker="Résultat attendu"><p>{content.successDefinition || "À confirmer ensemble."}</p></EditorialSection><EditorialSection title="Jalons partagés" kicker="Roadmap">{content.milestones?.length ? <div className="divide-y divide-[#eeeae2]">{content.milestones.map((item, index) => <div key={index} className="grid gap-3 py-5 first:pt-0 last:pb-0 md:grid-cols-[1fr_160px_130px]"><div><div className="font-semibold text-[#202b28]">{item.milestone}</div>{item.dependency ? <div className="mt-1 text-[14px] text-[#79817e]">Dépendance · {item.dependency}</div> : null}</div><div className="text-[14px]">{item.owner || "Responsable à définir"}</div><div className="text-[14px] font-medium text-[#5c51b3]">{item.dueDate || "Date à définir"}</div></div>)}</div> : <p className="italic text-[#8b938f]">Aucun jalon défini.</p>}</EditorialSection><EditorialSection title="Engagements réciproques" kicker="Responsabilités"><PairGrid leftTitle="Côté client" left={<BulletList items={content.clientCommitments} />} rightTitle="Côté Gando" right={<BulletList items={content.gandoCommitments} />} /></EditorialSection><EditorialSection title="Dépendances et risques" kicker="Points de vigilance"><PairGrid leftTitle="Dépendances" left={<BulletList items={content.dependencies} />} rightTitle="Risques" right={<BulletList items={content.risks} />} /></EditorialSection><EditorialSection title="Critères de passage à SD03" kicker="Definition of done"><BulletList items={content.exitCriteria} /></EditorialSection></div>; }
function SD03Document({ content }: { content: SD03Content }) { return <div className="space-y-5 sm:space-y-6"><Lead code="SD03">{content.solutionSummary || "Périmètre solution et intégration à confirmer."}</Lead><EditorialSection title="Périmètre de la solution" kicker="Scope"><PairGrid leftTitle="Inclus" left={<BulletList items={content.scopeIn} />} rightTitle="Hors périmètre" right={<BulletList items={content.scopeOut} />} /></EditorialSection><EditorialSection title="Intégrations et données" kicker="Architecture opérationnelle"><PairGrid leftTitle="Intégrations" left={<BulletList items={content.integrations} />} rightTitle="Données nécessaires" right={<BulletList items={content.dataRequirements} />} /></EditorialSection><EditorialSection title="Pilote" kicker="Mise à l’épreuve"><div className="grid gap-5 sm:grid-cols-2"><div><div className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#7a837f]">Périmètre</div><p className="mt-2">{content.pilot?.perimeter || "À définir"}</p></div><div><div className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#7a837f]">Durée</div><p className="mt-2">{content.pilot?.duration || "À définir"}</p></div></div><div className="mt-6 border-t border-[#eeeae2] pt-5"><h3 className="mb-3 text-[13px] font-semibold uppercase tracking-[0.08em] text-[#727b77]">Métriques de succès</h3><BulletList items={content.pilot?.successMetrics} /></div></EditorialSection><EditorialSection title="Sécurité, conformité et déploiement" kicker="Conditions d’exécution"><PairGrid leftTitle="Sécurité & conformité" left={<BulletList items={content.securityAndCompliance} />} rightTitle="Plan de déploiement" right={<BulletList items={content.deploymentPlan} />} />{content.technicalOwners?.length ? <div className="mt-6 border-t border-[#eeeae2] pt-5"><h3 className="mb-3 text-[13px] font-semibold uppercase tracking-[0.08em] text-[#727b77]">Responsables techniques</h3><BulletList items={content.technicalOwners} /></div> : null}</EditorialSection></div>; }
function SD04Document({ content }: { content: SD04Content }) { return <div className="space-y-5 sm:space-y-6"><Lead code="SD04">{content.offerSummary || "Offre commerciale à finaliser."}</Lead><EditorialSection title="Tarification" kicker="Proposition économique">{content.pricing?.length ? <div className="divide-y divide-[#eeeae2]">{content.pricing.map((row, index) => <div key={index} className="grid gap-2 py-5 first:pt-0 last:pb-0 sm:grid-cols-[1fr_1fr_150px]"><div className="font-semibold text-[#202b28]">{row.item}</div><div className="text-[14px]">{row.model}</div><div className="font-semibold text-[#5d52b6]">{row.price}</div>{row.notes ? <div className="text-[14px] text-[#7a837f] sm:col-span-3">{row.notes}</div> : null}</div>)}</div> : <p className="italic text-[#8b938f]">Tarification à renseigner.</p>}</EditorialSection><EditorialSection title="Business case" kicker="Valeur attendue">{content.businessCase?.length ? <div className="divide-y divide-[#eeeae2]">{content.businessCase.map((row, index) => <div key={index} className="grid gap-3 py-5 first:pt-0 last:pb-0 sm:grid-cols-[1fr_1fr]"><div className="font-semibold text-[#202b28]">{row.metric}</div><div><div className="text-[14px]">Actuel · {row.baseline || "—"}</div><div className="text-[14px]">Cible · {row.target || "—"}</div>{row.value ? <div className="mt-2 font-medium text-[#5d52b6]">{row.value}</div> : null}</div></div>)}</div> : <p className="italic text-[#8b938f]">Business case à chiffrer.</p>}</EditorialSection><EditorialSection title="Conditions et processus d’achat" kicker="Cadre commercial"><PairGrid leftTitle="Conditions commerciales" left={<BulletList items={content.commercialTerms} />} rightTitle="Achats / procurement" right={<BulletList items={content.procurementSteps} />} />{content.assumptions?.length ? <div className="mt-6 border-t border-[#eeeae2] pt-5"><h3 className="mb-3 text-[13px] font-semibold uppercase tracking-[0.08em] text-[#727b77]">Hypothèses</h3><BulletList items={content.assumptions} /></div> : null}</EditorialSection>{content.validityDate ? <p className="text-center text-[14px] text-[#727b77]">Offre valable jusqu’au <strong className="font-semibold text-[#313b38]">{content.validityDate}</strong></p> : null}</div>; }
function SD05Document({ content }: { content: SD05Content }) { return <div className="space-y-5 sm:space-y-6"><Lead code="SD05">{content.contractSummary || "Dernières validations avant signature."}</Lead><EditorialSection title="Points contractuels" kicker="Revue juridique">{content.legalItems?.length ? <div className="divide-y divide-[#eeeae2]">{content.legalItems.map((row, index) => <div key={index} className="py-5 first:pt-0 last:pb-0"><div className="flex flex-wrap items-center justify-between gap-3"><div className="font-semibold text-[#202b28]">{row.topic}</div><span className={`rounded-full border px-2.5 py-1 text-[11px] font-semibold ${row.status === "approved" ? "border-[#c8ddcf] bg-[#f1f8f3] text-[#2e6a40]" : "border-[#ddd8cd] bg-[#faf8f3] text-[#6e6a61]"}`}>{row.status === "approved" ? "Validé" : row.status === "in_review" ? "En revue" : "Ouvert"}</span></div><div className="mt-2 text-[14px] text-[#7a837f]">Responsable · {row.owner || "à définir"}</div>{row.notes ? <p className="mt-2">{row.notes}</p> : null}</div>)}</div> : <p className="italic text-[#8b938f]">Aucun point contractuel renseigné.</p>}</EditorialSection><EditorialSection title="Signataires" kicker="Pouvoirs de signature">{content.signatories?.length ? <div className="divide-y divide-[#eeeae2]">{content.signatories.map((row, index) => <div key={index} className="grid gap-2 py-4 first:pt-0 last:pb-0 sm:grid-cols-[1fr_1fr]"><div className="font-semibold text-[#202b28]">{row.name}</div><div className="text-[14px]">{row.role}{row.organization ? ` · ${row.organization}` : ""}</div></div>)}</div> : <p className="italic text-[#8b938f]">Signataires à confirmer.</p>}</EditorialSection><EditorialSection title="Signature et conditions finales" kicker="Closing"><PairGrid leftTitle="Étapes de signature" left={<NumberedList items={content.signatureSteps} />} rightTitle="Conditions finales" right={<BulletList items={content.finalConditions} />} /></EditorialSection><EditorialSection title="Handover et lancement" kicker="Après signature"><BulletList items={content.handoverPlan} />{content.goLiveDate ? <div className="mt-6 border-t border-[#eeeae2] pt-5"><span className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#7a837f]">Go-live cible</span><div className="mt-1 text-[18px] font-semibold text-[#202b28]">{content.goLiveDate}</div></div> : null}</EditorialSection></div>; }
function DocumentBody({ document }: { document: PublicDocument }) { if (document.code === "SD01") return <SD01Document content={document.content as SD01Content} />; if (document.code === "SD02") return <SD02Document content={document.content as SD02Content} />; if (document.code === "SD03") return <SD03Document content={document.content as SD03Content} />; if (document.code === "SD04") return <SD04Document content={document.content as SD04Content} />; return <SD05Document content={document.content as SD05Content} />; }

function AccessGate({ firstName, lastName, email, setFirstName, setLastName, setEmail, loading, error, onSubmit }: { firstName: string; lastName: string; email: string; setFirstName: (value: string) => void; setLastName: (value: string) => void; setEmail: (value: string) => void; loading: boolean; error: string; onSubmit: (event: React.FormEvent) => void }) { return <main className="min-h-screen bg-[#f6f3ec] px-5 py-8 sm:px-8 sm:py-14"><div className="mx-auto max-w-[1080px]"><header className="flex items-center justify-between"><div className="flex items-center gap-2.5"><GandoMark className="h-8 w-8" /><span className="text-sm font-semibold tracking-[-0.02em] text-[#1d2724]">Gando</span></div><span className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[#827d73]">Deal Room privée</span></header><div className="grid min-h-[70vh] items-center gap-10 py-12 lg:grid-cols-[1fr_430px] lg:gap-20"><div className="max-w-[620px]"><Eyebrow>Espace de collaboration</Eyebrow><h1 className="mt-5 text-[42px] font-medium leading-[1.08] tracking-[-0.045em] text-[#17211e] sm:text-[58px]">Un espace clair pour avancer sur les décisions qui comptent.</h1><p className="mt-6 max-w-[560px] text-[17px] leading-[1.75] text-[#626d68]">Retrouvez le cadrage du projet, les décisions prises, le plan d’action, la proposition commerciale et les prochaines validations au même endroit.</p><div className="mt-8 flex items-center gap-3 text-[13px] text-[#777f7b]"><ShieldCheck className="h-4 w-4 text-[#6558c8]" /> Accès identifié et confidentiel</div></div><form onSubmit={onSubmit} className="rounded-[22px] border border-[#e0dbd0] bg-[#fbfaf7] p-6 shadow-[0_18px_55px_rgba(54,48,36,0.07)] sm:p-8"><div className="text-[12px] font-semibold uppercase tracking-[0.12em] text-[#777268]">Accéder à la Room</div><h2 className="mt-2 text-[26px] font-semibold tracking-[-0.03em] text-[#1b2522]">Identifiez-vous</h2><p className="mt-2 text-[14px] leading-6 text-[#747d79]">Ces informations permettent d’attribuer les commentaires et validations.</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="block"><span className="text-[12px] font-semibold text-[#4e5854]">Prénom</span><input value={firstName} onChange={event => setFirstName(event.target.value)} required autoComplete="given-name" className="mt-2 h-11 w-full rounded-xl border border-[#dcd7cc] bg-white px-3.5 text-[15px] text-[#1e2825] outline-none transition focus:border-[#7b6fd0] focus:ring-2 focus:ring-[#7568cf]/10" /></label><label className="block"><span className="text-[12px] font-semibold text-[#4e5854]">Nom</span><input value={lastName} onChange={event => setLastName(event.target.value)} required autoComplete="family-name" className="mt-2 h-11 w-full rounded-xl border border-[#dcd7cc] bg-white px-3.5 text-[15px] text-[#1e2825] outline-none transition focus:border-[#7b6fd0] focus:ring-2 focus:ring-[#7568cf]/10" /></label></div><label className="mt-4 block"><span className="text-[12px] font-semibold text-[#4e5854]">Email professionnel</span><input type="email" value={email} onChange={event => setEmail(event.target.value)} required autoComplete="email" className="mt-2 h-11 w-full rounded-xl border border-[#dcd7cc] bg-white px-3.5 text-[15px] text-[#1e2825] outline-none transition focus:border-[#7b6fd0] focus:ring-2 focus:ring-[#7568cf]/10" /></label>{error ? <p className="mt-4 rounded-xl bg-[#fff3f1] px-3.5 py-3 text-[13px] text-[#9a4137]">{error}</p> : null}<button type="submit" disabled={loading} className="mt-6 flex h-11 w-full items-center justify-center gap-2 rounded-xl bg-[#202a27] px-4 text-[14px] font-semibold text-white transition hover:bg-[#303b37] disabled:opacity-55">{loading ? <Loader2 className="h-4 w-4 animate-spin" /> : null} Entrer dans la Room <ChevronRight className="h-4 w-4" /></button></form></div></div></main>; }

export function PublicSDRoomV4({ token }: { token: string }) {
  const [firstName, setFirstName] = useState(""); const [lastName, setLastName] = useState(""); const [email, setEmail] = useState(""); const [data, setData] = useState<PublicRoomData | null>(null); const [activeStage, setActiveStage] = useState<SDCode>("SD01"); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [sessionId, setSessionId] = useState(""); const [comment, setComment] = useState(""); const [commentState, setCommentState] = useState<"idle" | "sending" | "sent">("idle"); const [validating, setValidating] = useState(false); const openedRef = useRef(false);
  useEffect(() => { const sid = sessionStorage.getItem(`gando-room-session:${token}`) || crypto.randomUUID(); sessionStorage.setItem(`gando-room-session:${token}`, sid); setSessionId(sid); setFirstName(sessionStorage.getItem(`gando-room-first:${token}`) || ""); setLastName(sessionStorage.getItem(`gando-room-last:${token}`) || ""); setEmail(sessionStorage.getItem(`gando-room-email:${token}`) || ""); }, [token]);
  const visitorEmail = data?.visitorEmail || "";
  const track = useCallback((eventType: string, documentCode: SDCode | null, activeSeconds = 0) => { if (!visitorEmail || !sessionId) return; void fetch(`/api/public/deal-room/${encodeURIComponent(token)}/events`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email: visitorEmail, sessionId, eventType, documentCode, activeSeconds, metadata: { firstName, lastName } }), keepalive: true }); }, [firstName, lastName, sessionId, token, visitorEmail]);
  const unlock = async (event: React.FormEvent) => { event.preventDefault(); if (!firstName.trim() || !lastName.trim() || !email.trim()) return; setLoading(true); setError(""); try { const response = await fetch(`/api/public/deal-room/${encodeURIComponent(token)}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ firstName: firstName.trim(), lastName: lastName.trim(), email: email.trim() }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Accès impossible"); setData(payload); const available = (payload.documents as PublicDocument[]).map(item => item.code); setActiveStage(available.includes(payload.room.currentStage) ? payload.room.currentStage : available[0] || "SD01"); sessionStorage.setItem(`gando-room-first:${token}`, firstName.trim()); sessionStorage.setItem(`gando-room-last:${token}`, lastName.trim()); sessionStorage.setItem(`gando-room-email:${token}`, payload.visitorEmail); } catch (unlockError) { setError(unlockError instanceof Error ? unlockError.message : "Accès impossible"); } finally { setLoading(false); } };
  useEffect(() => { if (!data || !sessionId || openedRef.current) return; openedRef.current = true; track("room_opened", activeStage); }, [activeStage, data, sessionId, track]);
  useEffect(() => { if (data && sessionId) track("stage_viewed", activeStage); }, [activeStage, data, sessionId, track]);
  useEffect(() => { if (!data || !sessionId) return; const interval = window.setInterval(() => { if (document.visibilityState === "visible") track("heartbeat", activeStage, 30); }, 30000); return () => window.clearInterval(interval); }, [activeStage, data, sessionId, track]);
  const currentDocument = data?.documents.find(document => document.code === activeStage) || data?.documents[0];
  const stages = useMemo(() => SD_CODES.map(code => ({ code, document: data?.documents.find(document => document.code === code) })), [data]);
  const validatedCount = data?.documents.filter(document => document.status === "validated").length || 0;
  const sendComment = async (event: React.FormEvent) => { event.preventDefault(); if (!comment.trim() || !data || !currentDocument) return; setCommentState("sending"); try { const response = await fetch(`/api/public/deal-room/${encodeURIComponent(token)}/comments`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email: data.visitorEmail, firstName, lastName, documentCode: currentDocument.code, sectionKey: currentDocument.code.toLowerCase(), body: comment.trim() }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Remarque non enregistrée"); setComment(""); setCommentState("sent"); window.setTimeout(() => setCommentState("idle"), 2500); } catch { setCommentState("idle"); } };
  const validateStage = async () => { if (!data || !currentDocument || currentDocument.status !== "published") return; setValidating(true); try { const response = await fetch(`/api/public/deal-room/${encodeURIComponent(token)}/validate`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email: data.visitorEmail, firstName, lastName, documentCode: currentDocument.code }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Validation impossible"); setData(current => current ? { ...current, documents: current.documents.map(document => document.code === currentDocument.code ? payload.document : document) } : current); } finally { setValidating(false); } };
  if (!data) return <AccessGate firstName={firstName} lastName={lastName} email={email} setFirstName={setFirstName} setLastName={setLastName} setEmail={setEmail} loading={loading} error={error} onSubmit={unlock} />;
  if (!currentDocument) return <main className="grid min-h-screen place-items-center bg-[#f6f3ec] px-5 text-center"><div><h1 className="text-2xl font-semibold text-[#202b28]">Room en préparation</h1><p className="mt-2 text-[#6f7874]">Aucune étape n’est encore publiée.</p></div></main>;
  const validatedBy = [currentDocument.validated_by_first_name, currentDocument.validated_by_last_name].filter(Boolean).join(" ");
  return <main className="min-h-screen bg-[#f6f3ec] text-[#1c2522]"><header className="sticky top-0 z-50 border-b border-[#e6e1d7]/90 bg-[#fbfaf7]/95 backdrop-blur-md"><div className="mx-auto flex h-16 max-w-[1180px] items-center gap-4 px-5 sm:px-7"><div className="flex min-w-0 items-center gap-2.5"><GandoMark className="h-8 w-8 shrink-0" /><span className="hidden text-sm font-semibold sm:inline">Gando</span><span className="text-[#bbb4a8]">/</span><span className="truncate text-sm font-medium text-[#4c5652]">{data.room.companyName}</span></div><div className="ml-auto hidden items-center gap-2 text-[12px] text-[#737b77] sm:flex"><LockKeyhole className="h-3.5 w-3.5" /> Room privée</div><div className="h-7 w-px bg-[#e2ddd3]" /><div className="text-right text-[11px] leading-4 text-[#737b77]"><div className="font-semibold text-[#38423f]">{firstName} {lastName}</div><div className="hidden sm:block">{data.visitorEmail}</div></div></div></header><section className="border-b border-[#e4dfd4] bg-[#fbfaf7]"><div className="mx-auto grid max-w-[1180px] gap-10 px-5 py-12 sm:px-7 sm:py-16 lg:grid-cols-[1fr_360px] lg:items-center lg:py-20"><div className="max-w-[720px]"><Eyebrow>Strategic Deal Room</Eyebrow><h1 className="mt-4 text-[42px] font-medium leading-[1.08] tracking-[-0.045em] text-[#17211e] sm:text-[56px]">{data.room.companyName} × Gando</h1><p className="mt-5 max-w-[650px] text-[17px] leading-[1.75] text-[#626d68]">{data.room.displaySubtitle || "Espace de collaboration stratégique"}</p><div className="mt-7 flex flex-wrap items-center gap-x-5 gap-y-2 text-[12px] text-[#7c837f]"><span>Mis à jour le {formatDate(data.room.updatedAt)}</span><span className="h-1 w-1 rounded-full bg-[#bbb5aa]" /><span>{validatedCount}/{data.documents.length} étape{data.documents.length > 1 ? "s" : ""} validée{validatedCount > 1 ? "s" : ""}</span></div></div><div className="rounded-[22px] border border-[#e4dfd5] bg-[#f4f0e7] p-6">{data.room.bannerImageUrl ? <img src={data.room.bannerImageUrl} alt="" className="mb-6 h-36 w-full rounded-[15px] object-cover" /> : null}<div className="flex items-center gap-4"><Logo name={data.room.companyName} url={data.room.companyLogoUrl} /><div className="text-[#9a9489]">×</div><div className="flex items-center gap-2.5"><GandoMark className="h-11 w-11" /><span className="font-semibold tracking-[-0.02em] text-[#202a27]">Gando</span></div></div><p className="mt-5 text-[13px] leading-6 text-[#777f7b]">Un document vivant pour conserver l’alignement, les décisions et les prochaines étapes jusqu’à la signature.</p></div></div></section><div className="mx-auto grid max-w-[1180px] gap-8 px-5 py-9 sm:px-7 lg:grid-cols-[245px_minmax(0,780px)] lg:justify-between lg:py-12"><aside className="lg:sticky lg:top-24 lg:self-start"><div className="text-[11px] font-semibold uppercase tracking-[0.14em] text-[#807a70]">Parcours</div><nav className="mt-4 space-y-1.5">{stages.map(({ code, document }) => { const active = code === currentDocument.code; const enabled = Boolean(document); return <button key={code} type="button" disabled={!enabled} onClick={() => enabled && setActiveStage(code)} className={`group flex w-full items-start gap-3 rounded-xl px-3 py-3 text-left transition ${active ? "bg-[#ebe7dc]" : enabled ? "hover:bg-[#efebe2]" : "cursor-default opacity-45"}`}><span className={`mt-0.5 font-mono text-[11px] font-semibold ${active ? "text-[#584ead]" : "text-[#8b877f]"}`}>{code.slice(2)}</span><span className="min-w-0 flex-1"><span className={`block text-[13px] font-semibold leading-5 ${active ? "text-[#202a27]" : "text-[#57615d]"}`}>{SD_STAGE_META[code].title}</span><span className="mt-0.5 block text-[11px] text-[#8a918e]">{document?.status === "validated" ? "Validé" : document?.status === "published" ? "À valider" : "À venir"}</span></span>{document?.status === "validated" ? <CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-[#4f835e]" /> : active ? <ChevronRight className="mt-0.5 h-4 w-4 shrink-0 text-[#6558c8]" /> : null}</button>; })}</nav><div className="mt-7 border-t border-[#ddd8cd] pt-5 text-[12px] leading-5 text-[#7a827e]"><div className="font-semibold text-[#555f5b]">Besoin d’un ajustement ?</div><p className="mt-1">Ajoutez une remarque en bas de l’étape. Elle sera visible par l’équipe Gando.</p></div></aside><article className="min-w-0"><div className="mb-7 flex items-center justify-between gap-4 border-b border-[#ddd8cd] pb-5"><div><Eyebrow>{currentDocument.code}</Eyebrow><h2 className="mt-1 text-[30px] font-semibold tracking-[-0.035em] text-[#18221f]">{SD_STAGE_META[currentDocument.code].title}</h2></div><span className={`shrink-0 rounded-full border px-3 py-1.5 text-[11px] font-semibold ${currentDocument.status === "validated" ? "border-[#c5d9ca] bg-[#f0f6f1] text-[#366844]" : "border-[#dcd6cb] bg-[#faf8f3] text-[#6c6860]"}`}>{currentDocument.status === "validated" ? "Validé" : "À valider"}</span></div><DocumentBody document={currentDocument} /><section className="mt-7 rounded-[20px] border border-[#ddd8cd] bg-[#fbfaf7] p-5 sm:p-7">{currentDocument.status === "validated" ? <div className="flex gap-4"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-[#e8f2ea] text-[#3d7049]"><Check className="h-5 w-5" /></div><div><div className="font-semibold text-[#24302c]">Cette étape est validée</div><p className="mt-1 text-[14px] leading-6 text-[#68726e]">{validatedBy ? `Validée par ${validatedBy}` : "Validation enregistrée"}{currentDocument.validated_at ? ` · ${formatDate(currentDocument.validated_at)}` : ""}.</p></div></div> : <div className="grid gap-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><Eyebrow>Validation</Eyebrow><h3 className="mt-1 text-[21px] font-semibold tracking-[-0.02em] text-[#202b28]">Êtes-vous aligné avec cette étape ?</h3><p className="mt-2 text-[14px] leading-6 text-[#6b7571]">La validation confirme l’accord sur le contenu présenté et permet à l’équipe d’avancer vers l’étape suivante.</p></div><button type="button" onClick={() => void validateStage()} disabled={validating} className="flex h-11 shrink-0 items-center justify-center gap-2 rounded-xl bg-[#202a27] px-5 text-[13px] font-semibold text-white transition hover:bg-[#303b37] disabled:opacity-55">{validating ? <Loader2 className="h-4 w-4 animate-spin" /> : <Check className="h-4 w-4" />} Valider {currentDocument.code}</button></div>}</section><section className="mt-5 rounded-[20px] border border-[#e4dfd5] bg-[#f1ede5] p-5 sm:p-7"><div className="flex items-center gap-2"><MessageSquare className="h-4 w-4 text-[#6558c8]" /><h3 className="text-[15px] font-semibold text-[#26312e]">Ajouter une remarque</h3></div><p className="mt-2 text-[13px] leading-6 text-[#747d79]">Question, correction ou point à confirmer : votre message sera rattaché à {currentDocument.code}.</p><form onSubmit={sendComment} className="mt-4"><textarea value={comment} onChange={event => setComment(event.target.value)} rows={4} placeholder="Écrivez votre remarque…" className="w-full resize-y rounded-xl border border-[#d9d3c8] bg-[#fbfaf7] px-3.5 py-3 text-[15px] leading-6 text-[#28322f] outline-none transition placeholder:text-[#9a9f9c] focus:border-[#776bd0] focus:ring-2 focus:ring-[#776bd0]/10" /><div className="mt-3 flex items-center justify-between"><span className="text-[11px] text-[#8a918e]">Attribué à {firstName} {lastName}</span><button type="submit" disabled={!comment.trim() || commentState === "sending"} className="flex h-9 items-center gap-2 rounded-lg border border-[#cec8bc] bg-[#fbfaf7] px-3.5 text-[12px] font-semibold text-[#3f4945] transition hover:bg-white disabled:opacity-50">{commentState === "sending" ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : commentState === "sent" ? <Check className="h-3.5 w-3.5" /> : <ArrowUpRight className="h-3.5 w-3.5" />}{commentState === "sent" ? "Envoyé" : "Envoyer"}</button></div></form></section></article></div><footer className="border-t border-[#ded9cf] bg-[#fbfaf7] px-5 py-7 text-center text-[11px] text-[#848a87]">Document confidentiel · {data.room.companyName} × Gando</footer></main>;
}
